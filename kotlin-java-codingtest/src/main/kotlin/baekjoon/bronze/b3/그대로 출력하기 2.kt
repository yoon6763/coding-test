package baekjoon.bronze.b3

fun main() {
    val br = System.`in`.bufferedReader()
    while (true) {
        val line = br.readLine()
        if (line == null) break
        else println(line)
    }
}