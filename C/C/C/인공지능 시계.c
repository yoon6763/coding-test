#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main() {
	int hour, min, second;
	int plus;
	scanf("%d %d %d", &hour, &min, &second);
	scanf("%d", &plus);
	
	second += plus;
	if (second >= 60) {
		second += 
	}


	return 0;
}